MINIMUM COMPILER VERSION IS C++11

To compile:

make

To run:

./main [0,1,2]

Omitting second runtime parameters makes system run in Contiguous File System mode. For the others:

0 - runs in Contiguous Mode
1 - runs in Indexed Mode
2 - runs in Chained Mode



